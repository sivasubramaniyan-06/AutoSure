/**
 * AUTOSURE — Users Controller (Stubs)
 */

import type { Request, Response } from 'express';
import { AppError } from '../utils/AppError';

export const getProfile = async (_req: Request, _res: Response) => {
  throw new AppError('Not implemented', 501);
};

export const updateProfile = async (_req: Request, _res: Response) => {
  throw new AppError('Not implemented', 501);
};
